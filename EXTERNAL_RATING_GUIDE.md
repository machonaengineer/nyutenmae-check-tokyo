# 外部評価スナップショット運用ガイド

外部評価は、店舗や場所の安全性を判断する指標ではなく、外部サービス上の集計参考値として扱う。

## 入力してよい情報

- 外部サービス名
- 出典URL
- 確認日
- 集計点数
- 評価件数
- 公式APIまたは管理者の目視確認で得た集計値
- 独自メモ

## 入力しない情報

- 口コミ本文
- 投稿者名
- 口コミ画像
- Google口コミ、食べログ、SNS、ニュース本文の転載
- スクレイピングHTML

## 表示方針

- `display_allowed=false` を初期値とし、公開可否を管理者が確認する。
- 食べログは許諾確認まで公開表示しない。
- Googleは公式APIまたは管理者目視確認に限定し、帰属表示と利用規約を確認する。
- 公開ページでは「外部評価と本サービスの注意報告は評価軸が異なります」と明記する。

## フェーズ6までの実装範囲

1. 外部評価ソースとスナップショットをDBで分離管理する。
2. 公開ビューは `display_allowed=true` かつ承認済み投稿がある場所だけを返す。
3. 管理画面から手動で出典URL、確認日、集計評価、評価件数を登録できる。
4. `GOOGLE_PLACES_API_KEY` 設定時だけ、管理画面からGoogle Places APIで集計値を同期できる。
5. 場所詳細ページでは、注意報告とは別枠の「外部評価参考値」として表示する。
6. CSV、運用ガイド、公開前チェック、Playwrightチェックで転載は禁止とRLS方針を確認する。

## CSV投入

`EXTERNAL_RATING_TEMPLATE.csv` を使い、以下を守る。

- `place_id` は既存の `places.id` を指定する。
- `source_slug` は `google_maps`, `tabelog`, `other_review_site` のいずれかを使う。
- `collection_method` は `manual`, `official_api`, `admin_note` のいずれかを使う。
- `checked_at` はISO形式または `YYYY-MM-DD` で記録する。
- `display_allowed` は明示的に `true` としたものだけ公開候補にする。

## 禁止事項

外部口コミ本文をコピーして公開本文、非公開メモ、CSV、DBへ貼り付けない。出典URL、確認日、独自要約、集計値だけを扱う。
