# フェーズ26: エリア別情報蓄積オペレーション管理

## 目的

12エリアへ拡大した後も、個別店舗への断定や未承認情報の公開を避けながら、情報蓄積、公式ソース確認、建物確認、コンテンツ増強を継続できる状態にする。

## 実装

- `/admin/area-ops` を追加し、管理者だけがエリア別の運用タスクを確認できるようにした。
- `src/lib/area-operations.ts` で、公式ソース、投稿導線、建物確認、コンテンツ増強を分けて扱う。
- 運用ステータスは `未調査`、`調査中`、`審査待ち`、`公開候補`、`保留` に整理した。
- 公式ソースは最終確認日と次回確認目安を表示する。
- `scripts/check-source-links.mjs` を追加し、`SOURCE_RESEARCH_QUEUE.csv` のURLを無料範囲で手動確認できるようにした。

## 公開安全方針

- エリア運用は公開判断ではない。
- 投稿は `pending / Hidden` から開始し、承認済み投稿だけを公開する。
- 投稿者メール、証拠画像URL、証拠ファイルの保存パス、非公開メモは公開しない。
- 外部口コミ、報道本文、SNS本文、画像、スクリーンショットは転載しない。
- 同一住所・同一建物は確認候補であり、同一運営や同一店舗とは断定しない。

## 運用

1. `/admin/area-ops` でエリア別タスクを確認する。
2. `公式ソース` はURL、確認日、独自要約を確認する。
3. `投稿導線` はエリア別情報提供ページと投稿フォームの導線を確認する。
4. `建物確認` は住所、建物名、階数、同一住所候補を管理者確認に留める。
5. `コンテンツ増強` は入店前確認、記録保存、相談導線に一般化して反映する。
6. 必要に応じて `npm run check:sources` を実行し、リンク切れや再確認が必要な公式URLを洗い出す。

## 次の改善候補

- エリア運用タスクをDB化し、管理者操作ログと連動する。
- 週次リンクチェックをVercel CronまたはGitHub Actionsで無料枠内に設定する。
- エリア別の投稿数、審査待ち数、公開件数を `/admin/area-ops` に統合する。
- 情報提供フォーム送信時に、エリア運用タスクの `投稿導線` に集計を反映する。
