# 運用SOP

## 基本方針

投稿は、店舗や個人への評価ではなく、投稿者の申告に基づく注意報告として扱う。公開前に、表現、個人情報、証拠レベル、リスクタグ、異議申立て可能性を確認する。

## 投稿審査フロー

1. `/admin/reports` で `pending` または `needs_review` の投稿を確認する。
2. 対象エリア、店名または住所、来店日時、会計内容、明細提示、退店時対応を確認する。
3. 証拠画像がある場合は、管理画面の署名付きURLで確認する。
4. 証拠画像に顔、個人名、電話番号、カード番号、SNS IDなどが含まれる場合は、公開本文に転記しない。
5. JPEG、PNG、WebP以外の画像はメタデータが残る可能性があるため、公開判断前に端末側の画像情報表示や安全なビューアで位置情報、撮影者名、端末名が含まれていないか確認する。
6. ウイルススキャンは未自動化のため、疑わしい添付や拡張子偽装がある場合はダウンロードせず非公開維持にする。
7. 公開サマリーを、断定を避けた表現へ整える。
8. リスクタグを必要最小限に設定する。
9. 証拠レベルを `S/A/B/C/D` から選ぶ。公開できない場合は `Hidden` のままにする。
10. 公開可能な場合だけ `approved` に変更する。
11. 判断に迷う場合は `needs_review` を維持する。

## 連投、honeypot対応

1. 投稿フォームと異議申立てフォームは、同一IP、同一メールアドレス、同一ブラウザ相当の短時間連投を制限する。
2. honeypot項目に値が入った送信は保存しない。発生頻度が増えた場合はアクセス元、送信時刻、対象フォームを確認する。
3. 正常利用者から制限解除の連絡があった場合は、メールアドレスと送信時刻を確認し、必要に応じて時間を置いて再送してもらう。
4. 自動投稿が増えた場合は `CAPTCHA_FUTURE_NOTES.md` に従い、Cloudflare TurnstileまたはhCaptchaをServer Actionに追加する。

## ステータス運用

| status | 用途 |
| --- | --- |
| `pending` | 投稿直後の非公開状態 |
| `needs_review` | 追加確認や表現修正が必要 |
| `approved` | 一般公開可能 |
| `hidden` | 公開後または審査中に非公開化 |
| `rejected` | 公開しない判断 |

## 証拠レベル運用

| level | 目安 |
| --- | --- |
| `S` | 複数資料や公的情報など、確認材料が強い |
| `A` | 領収書、明細、写真など具体的資料がある |
| `B` | 経緯が具体的で追加確認余地がある |
| `C` | 投稿者申告が中心 |
| `D` | 情報が限定的 |
| `Hidden` | 非公開管理用 |

## 異議申立て対応

1. `/admin/objections` で新規申立てを確認する。
2. 対象URL、投稿ID、申立て内容、関係性、連絡先を確認する。
3. 個人情報や権利侵害の懸念が高い場合、対象投稿を一時的に `hidden` にする。
4. 投稿内容、証拠、公開サマリー、リスクタグを再確認する。
5. 必要に応じて公開サマリー修正、タグ変更、非公開化、却下を行う。
6. 対応ステータスを `reviewing`, `resolved`, `rejected` のいずれかに更新する。
7. 重要判断は `admin_actions` に残る操作ログとあわせて、運用メモにも記録する。

## 削除依頼、店舗側反論、補足資料の扱い

1. 削除依頼を受けた場合、まず対象投稿を特定し、個人情報や権利侵害の可能性が高ければ一時的に `hidden` にする。
2. 店舗側反論や補足説明は、申立て者の関係性を確認し、公開本文へそのまま転載しない。
3. 店舗側から提出された資料、担当者名、連絡先、請求書、画像は管理者確認用として扱い、一般公開しない。
4. 表示継続、修正、非公開化、削除の判断理由を `admin_actions` の操作ログと運用メモに残す。
5. 削除相当と判断した場合は、投稿ステータスを `hidden` または `rejected` に変更し、公開ページから表示されないことを確認する。

## 事故時対応

### 証拠画像が公開された疑い

1. Storage bucket `report-evidence-files` が private か確認する。
2. 問題のある投稿を `hidden` に変更する。
3. 署名付きURLの共有範囲とログを確認する。
4. 必要に応じてファイルを削除し、再アップロードを依頼する。

### Service Role Key漏えいの疑い

1. SupabaseでService Role Keyを再発行する。
2. Vercelの `SUPABASE_SERVICE_ROLE_KEY` を更新する。
3. 古いキーを無効化する。
4. 管理操作ログ、投稿作成履歴、Storage操作を確認する。

### 不適切な公開表示の疑い

1. 対象投稿を `hidden` にする。
2. 公開ビュー、地図、詳細ページから非表示になったことを確認する。
3. 公開サマリー、リスクタグ、証拠レベルを見直す。
4. 必要に応じて異議申立て対応フローへ移行する。

## 定期点検

- 週1回: `pending` と `needs_review` の滞留確認
- 週1回: 異議申立て未対応確認
- 月1回: RLS/Storage private設定確認
- 月1回: `npm audit --audit-level=moderate`
- 月1回: 禁止表現と危険表現検知リストの見直し
