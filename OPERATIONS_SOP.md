# 運用SOP

## 基本方針

投稿は、店舗や個人への評価ではなく、投稿者の申告に基づく注意報告として扱う。公開前に、表現、個人情報、証拠レベル、リスクタグ、異議申立て可能性を確認する。

## 投稿審査フロー

1. `/admin` で未審査、異議申立て、公開状況を確認する。
2. `/admin/quality` で建物情報不足、出典確認待ち、7日超未審査、未対応異議を確認する。
3. `/admin/reports` で `pending` または `needs_review` の投稿を確認する。
4. 対象エリア、店名または住所、来店日時、会計内容、明細提示、退店時対応を確認する。
5. 証拠画像がある場合は、管理画面の署名付きURLで確認する。
6. 証拠画像に顔、個人名、電話番号、カード番号、SNS IDなどが含まれる場合は、公開本文に転記しない。
7. JPEG、PNG、WebP以外の画像はメタデータが残る可能性があるため、公開判断前に端末側の画像情報表示や安全なビューアで位置情報、撮影者名、端末名が含まれていないか確認する。
8. ウイルススキャンは未自動化のため、疑わしい添付や拡張子偽装がある場合はダウンロードせず非公開維持にする。
9. 公開サマリーを、断定を避けた表現へ整える。
10. リスクタグを必要最小限に設定する。
11. 証拠レベルを `S/A/B/C/D` から選ぶ。公開できない場合は `Hidden` のままにする。
12. 公開可能な場合だけ `approved` に変更する。
13. 判断に迷う場合は `needs_review` を維持する。

## 初期データCSV検証

1. `INITIAL_DATA_TEMPLATE.csv` に沿って初期データを整理する。
2. `/admin/data` にCSVを貼り付け、列、status、証拠レベル、危険表現、転載リスクを確認する。
3. エラーがある行は投入しない。
4. 警告がある行は、出典、確認日、独自要約、公開可否を人間が再確認する。
5. 初期投入時は原則 `pending` / `Hidden` とし、公開前に個別審査する。
6. 管理画面の投入ボタンは `pending` または `needs_review`、証拠レベル `Hidden` の行だけを保存する。
7. 投入後は `/admin/reports` で対象行を確認し、公開前に通常の投稿審査フローへ戻す。
8. 重複スキップが発生した場合は、同じエリア、場所名、公開サマリーの既存投稿を確認する。

## 公的・公式ソース調査

1. `/admin/research` または `SOURCE_RESEARCH_QUEUE.csv` で優先度の高いソースから確認する。
2. 公式ページを開き、URL、確認日、確認者、利用目的を記録する。
3. 本文をコピーせず、独自要約、出典URL、確認日として整理する。
4. 個別店舗への注意報告にする場合は、具体的な根拠と管理者審査がある場合に限る。
5. 店舗名や住所が確認できないエリア単位の情報は、`/sources`、相談導線、エリア説明の改善に使う。
6. 外部口コミ、ニュース本文、SNS投稿本文、画像、スクリーンショットは保存しない。
7. `source_type`、`source_url`、`source_title`、`source_checked_at` を入力し、公開前に `source_title` と公開サマリーへ禁止表現や非公開情報が入っていないことを確認する。
8. 投稿者申告ではない出典確認候補を承認する場合は、公開ページに出典種別、出典URL、確認日が出ることを確認する。
9. `/coverage` の件数は出典整理と審査進捗の目安であり、個別店舗の安全性や問題有無を断定する表示にしない。

## 情報提供ページからの投稿確認

1. `/contribute` とエリア×種別ページから来た投稿も、通常の投稿と同じく `pending` / `Hidden` で審査する。
2. URLパラメータで事前選択されたエリアやタグは、投稿者の入力補助であり、公開判断の根拠にはしない。
3. エリア×種別ページは一般的な確認項目であり、承認済み投稿がない場合は空状態のまま維持する。
4. 投稿数を増やす目的でも、未承認投稿や外部口コミ本文を公開ページに出さない。

## 連投、honeypot対応

1. 投稿フォームと異議申立てフォームは、同一IP、同一メールアドレス、同一ブラウザ相当の短時間連投を制限する。
2. honeypot項目に値が入った送信は保存しない。発生頻度が増えた場合はアクセス元、送信時刻、対象フォームを確認する。
3. 正常利用者から制限解除の連絡があった場合は、メールアドレスと送信時刻を確認し、必要に応じて時間を置いて再送してもらう。
4. 自動投稿が増えた場合は `CAPTCHA_FUTURE_NOTES.md` に従い、Cloudflare TurnstileまたはhCaptchaをServer Actionに追加する。

## ステータス運用

| status | 用途 |
| --- | --- |
| `pending` | 投稿直後の非公開状態 |
| `needs_review` | 追加確認や表現修正が必要 |
| `approved` | 一般公開可能 |
| `hidden` | 公開後または審査中に非公開化 |
| `rejected` | 公開しない判断 |

## 証拠レベル運用

| level | 目安 |
| --- | --- |
| `S` | 複数資料や公的情報など、確認材料が強い |
| `A` | 領収書、明細、写真など具体的資料がある |
| `B` | 経緯が具体的で追加確認余地がある |
| `C` | 投稿者申告が中心 |
| `D` | 情報が限定的 |
| `Hidden` | 非公開管理用 |

## 異議申立て対応

1. `/admin/objections` で新規申立てを確認する。
2. 対象URL、投稿ID、申立て内容、関係性、連絡先を確認する。
3. 個人情報や権利侵害の懸念が高い場合、対象投稿を一時的に `hidden` にする。
4. 投稿内容、証拠、公開サマリー、リスクタグを再確認する。
5. 必要に応じて公開サマリー修正、タグ変更、非公開化、却下を行う。
6. 対応ステータスを `reviewing`, `resolved`, `rejected` のいずれかに更新する。
7. 重要判断は `admin_actions` に残る操作ログとあわせて、運用メモにも記録する。

## 外部評価参考値の運用

1. 外部評価は、本サービスの評価ではなく、外部サービス上の集計参考値として扱う。
2. 入力できるのは、外部サービス名、出典URL、確認日、集計評価、評価件数、取得方法、管理者メモに限定する。
3. 口コミ本文、投稿者名、口コミ画像、スクリーンショット、スクレイピングHTMLは保存しない。
4. Googleは公式Places APIまたは管理者の目視確認に限定し、帰属表示と利用規約を確認する。
5. 食べログなど許諾や規約確認が必要なソースは、確認が終わるまで `display_allowed=false` のまま公開しない。
6. 外部評価を追加した場合は、公開ページに集計値、出典URL、確認日だけが表示され、注意報告とは評価軸が異なる旨が表示されることを確認する。
7. 公開後に規約、許諾、出典表示に疑義が出た場合は、対象スナップショットを `display_allowed=false` に戻し、必要に応じて公開ページを再確認する。

## 計測・収益化枠の運用

1. Vercel Web Analyticsは `NEXT_PUBLIC_VERCEL_ANALYTICS_ENABLED=true` の場合だけ読み込む。
2. 収益化枠は `NEXT_PUBLIC_MONETIZATION_ENABLED=true` の場合だけ表示する。初期運用ではOFFを維持する。
3. 広告、スポンサー、支援リンクは、投稿審査、公開順位、リスクタグ、証拠レベルに影響させない。
4. 収益化を開始する前に、ホスティングプランの商用利用条件、広告サービスのポリシー、法務文面を確認する。
5. 収益化枠に、店舗や個人への断定表現、外部口コミ本文、証拠画像、投稿者メールアドレスを含めない。
6. AdSenseを使う場合、アカウントのログイン情報、本人確認情報、支払い情報はチャット、Issue、README、環境変数名以外の場所に残さない。
7. AdSenseを使う場合、まず `ADS_TXT_GOOGLE_PUBLISHER_ID` だけを設定し、`/ads.txt` を確認してから審査へ進める。
8. AdSense広告は、当面 `/checklists` と `/areas/[slug]/checklist` の情報整理ページだけに置く。投稿フォーム、異議申立てフォーム、管理画面には置かない。
9. 広告の周辺にクリック依頼、支援依頼、矢印、強調アニメーション、誤認しやすい見出しを置かない。
10. AdSenseポリシー警告、無効なトラフィック警告、Cookie表示の不備が出た場合は、`NEXT_PUBLIC_ADSENSE_ENABLED=false` に戻し、原因確認後に再開する。

## SNS運用

1. `/admin/social` の文面テンプレートを使い、SNSへ投稿する前に内容を確認する。
2. SNSには証拠画像、投稿者メールアドレス、非公開メモ、個人情報を載せない。
3. Google口コミ、食べログ、SNS、ニュース本文をそのまま転載しない。
4. 個別報告を紹介する場合は、承認済み公開ページのURLだけを共有し、断定表現を追加しない。
5. SNSから情報提供が来た場合は、投稿フォームまたは異議申立てフォームへ誘導し、DM内の個人情報を公開しない。
6. 自動投稿API連携は、アクセストークン保管、投稿前承認、操作ログ、停止手順を実装するまで行わない。

## スポンサー問い合わせ対応

1. `/admin/sponsors` でスポンサー、広告、連携、支援の問い合わせを確認する。
2. 問い合わせ内容は公開ページへ表示しない。
3. 掲載可否は、掲載独立性、広告サービス規約、法務文面、プライバシー表示を確認してから判断する。
4. スポンサーや広告主に、投稿審査、公開順位、リスクタグ、証拠レベルへ関与させない。
5. 個別店舗や投稿に対する表示操作、非公開化、タグ編集などを対価として受けない。
6. 問い合わせに個人情報や機密情報が含まれる場合は、必要最小限の管理者だけで扱う。

## 削除依頼、店舗側反論、補足資料の扱い

1. 削除依頼を受けた場合、まず対象投稿を特定し、個人情報や権利侵害の可能性が高ければ一時的に `hidden` にする。
2. 店舗側反論や補足説明は、申立て者の関係性を確認し、公開本文へそのまま転載しない。
3. 店舗側から提出された資料、担当者名、連絡先、請求書、画像は管理者確認用として扱い、一般公開しない。
4. 表示継続、修正、非公開化、削除の判断理由を `admin_actions` の操作ログと運用メモに残す。
5. 削除相当と判断した場合は、投稿ステータスを `hidden` または `rejected` に変更し、公開ページから表示されないことを確認する。

## 事故時対応

### 証拠画像が公開された疑い

1. Storage bucket `report-evidence-files` が private か確認する。
2. 問題のある投稿を `hidden` に変更する。
3. 署名付きURLの共有範囲とログを確認する。
4. 必要に応じてファイルを削除し、再アップロードを依頼する。

### Service Role Key漏えいの疑い

1. SupabaseでService Role Keyを再発行する。
2. Vercelの `SUPABASE_SERVICE_ROLE_KEY` を更新する。
3. 古いキーを無効化する。
4. 管理操作ログ、投稿作成履歴、Storage操作を確認する。

### 不適切な公開表示の疑い

1. 対象投稿を `hidden` にする。
2. 公開ビュー、地図、詳細ページから非表示になったことを確認する。
3. 公開サマリー、リスクタグ、証拠レベルを見直す。
4. 必要に応じて異議申立て対応フローへ移行する。

## 定期点検

- 週1回: `pending` と `needs_review` の滞留確認
- 週1回: 異議申立て未対応確認
- 週1回: `/healthz` が `status: ok` を返すこと、Vercel本番デプロイが `Ready` であることを確認する
- 月1回: RLS/Storage private設定確認
- 月1回: `npm audit --audit-level=moderate`
- 月1回: 禁止表現と危険表現検知リストの見直し
- 月1回: AdSenseを有効化している場合、ads.txt、広告配置、プライバシーポリシー、無効クリック警告の有無を確認する
