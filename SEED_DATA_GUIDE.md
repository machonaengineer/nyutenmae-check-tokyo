# 初期データ投入ガイド

このガイドは、`INITIAL_DATA_TEMPLATE.csv` を使って初期データを整理するための運用メモです。MVPでは、外部情報をそのまま転載せず、確認日、出典URL、独自要約、リスクタグとして扱います。

外部サービス上の星評価や件数を参考値として扱う場合は、`EXTERNAL_RATING_TEMPLATE.csv` と `EXTERNAL_RATING_GUIDE.md` を使い、注意報告データとは分けて管理します。

## 対象にできる情報

- 公的機関の公開情報
- 報道機関の記事の見出し、URL、公開日、確認日
- 自社に届いたタレコミや問い合わせ
- Google口コミ、食べログ、SNSなど外部口コミで見られる傾向

## 禁止する扱い

- Google口コミ、食べログ、SNS、ニュース本文の転載は禁止
- 外部口コミ本文、投稿者名、画像、スクリーンショット、スクレイピングHTMLの保存
- 店舗や個人への断定、侮辱、個人攻撃を含む要約
- 投稿者メールアドレス、レシート、明細、証拠画像URLの公開列への転記
- 店員個人名、顔写真、電話番号、SNS IDなど個人を特定し得る情報の公開

## CSV列の使い方

| 列 | 入力方針 |
| --- | --- |
| `source_type` | `user_report`, `public_agency`, `police`, `consumer_center`, `municipality`, `news`, `internal_tip`, `external_review_trend`, `other` のいずれか |
| `source_url` | 出典URL。自社タレコミなどURLがない場合は空欄 |
| `source_title` | 出典のタイトルや内部受付名 |
| `source_checked_at` | 確認日。`YYYY-MM-DD` |
| `observed_area` | 新宿・歌舞伎町、池袋、渋谷・道玄坂・宇田川町、上野・御徒町・湯島 |
| `place_name` | 店名または場所の手がかり |
| `address` | 分かる範囲の住所 |
| `building_name` | 建物名 |
| `floor` | 階数 |
| `incident_type` | 料金説明、会計確認、明細提示、退店時対応など |
| `risk_tags` | `;` 区切りでリスクタグを記入 |
| `evidence_level` | 初期は原則 `Hidden`。管理者確認後に `S/A/B/C/D` |
| `public_summary` | 独自要約。断定を避け、投稿者の申告または確認範囲として記述 |
| `private_memo` | 非公開メモ。証拠や確認経緯を記録 |
| `status` | 初期は `pending` または `needs_review` |
| `reviewed_by_admin` | 管理者確認済みならメールアドレスまたは管理者ID |
| `published_at` | 公開済みの場合のみ日時 |

## 投入前チェック

1. 初期投入する行の `status` は `pending` または `needs_review` にし、自動公開しないこと。
2. `public_summary` に禁止表現や個人情報がないこと。
3. `private_memo` に証拠情報や投稿者連絡先を入れる場合、一般公開しない運用であること。
4. 外部情報は出典URLと確認日を残し、本文転載は禁止していないこと。
5. `source_title` は公開される可能性があるため、強い断定、個人情報、外部本文のコピーを含めないこと。
6. 管理者が公開サマリー、リスクタグ、証拠レベルを確認していること。

## 管理画面からの投入

1. `/admin/data` にCSVを貼り付ける。
2. 自動検証のエラーを0件にする。
3. `status` は `pending` または `needs_review`、`evidence_level` は `Hidden` の行だけを投入する。
4. 投入後は `/admin/reports` で審査待ち投稿として確認する。
5. 公開する場合は、投稿詳細画面で表現、個人情報、リスクタグ、証拠レベルを人間が確認してから `approved` に変更する。

管理画面の投入機能は、CSV上の行をそのまま公開しません。外部情報の傾向や自社確認メモは、まず非公開投稿として保存し、公開判断は通常の投稿審査フローで行います。

## 推奨する公開表現

- 投稿者の申告に基づく情報です。
- 事実確認中の情報を含みます。
- 入店前の料金確認を推奨します。
- 料金説明と会計内容の不一致報告あり。
- 会計時対応に関する注意報告あり。
