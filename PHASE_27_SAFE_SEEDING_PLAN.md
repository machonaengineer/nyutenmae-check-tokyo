# フェーズ27: 安全な初期データ投入の実作業化

## 目的

情報が少ない状態でも、根拠のない個別店舗情報を作らず、公式ソース由来のエリア単位候補を非公開の審査DBへ登録できるようにする。

## 実装

- `OFFICIAL_SOURCE_SEED_CANDIDATES.csv` を追加した。
- `src/lib/admin/official-area-seed-candidates.ts` で、公式ソース由来のエリア候補CSVと件数を管理する。
- `/admin/data` に `公式ソース安全候補` パネルを追加した。
- 管理者は `/admin/data` から、12エリア分の公式ソース候補を `initial_data_review_candidates` へ登録できる。
- 登録候補は `needs_review / Hidden` 固定で、公開ページにも承認済み投稿にもならない。
- `npm run check:official-seed` を追加し、公式候補CSVの列、ステータス、証拠レベル、禁止表現、非公開情報マーカーを検査できる。

## 公開安全方針

- 個別店舗の注意報告を根拠なしに作らない。
- 公式ソース候補はエリア単位の確認観点として扱う。
- 投稿者メール、証拠画像URL、証拠ファイルの保存パス、非公開メモは公開しない。
- 外部口コミ本文、報道本文、SNS本文、画像、スクリーンショットは転載しない。
- 候補審査DBへの登録は公開承認ではない。
- 公開する場合も、通常の投稿審査、出典確認、法務・表現確認、異議申立て導線確認を通す。

## 運用

1. `npm run check:official-seed` を実行し、CSVが安全条件を満たすことを確認する。
2. `/admin/data` を開く。
3. `公式ソース安全候補` の件数、対象エリア、公式URL数、Hidden固定件数を確認する。
4. `公式候補を審査DBへ登録` を押す。
5. 登録後、候補審査DBで `出典確認`、`公開サマリーが独自要約`、`法務・表現確認` を進める。
6. 個別店舗として扱う根拠がない候補は、公開投稿へ進めず、エリアページ、チェックリスト、記録保存ガイド、相談導線の改善に使う。

## 次の改善候補

- 候補審査DBに `area_content_action` のようなエリア改善用の判断を追加する。
- `/admin/area-ops` と候補審査DBを連動し、登録済み候補数をエリア別に表示する。
- 公式ソースのリンク切れ確認を週次で自動化する。
- 本人投稿が入ったエリアだけ、候補審査DBから通常の投稿審査へ進めるための導線を強化する。
