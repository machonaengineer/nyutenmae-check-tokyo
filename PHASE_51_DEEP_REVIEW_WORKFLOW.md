# Phase 51: 初期データ審査ワークフローの実運用化

## 目的

フェーズ29〜50で定義した安全成長ロードマップを、実際に情報蓄積へつなげるため、初期データ候補を管理者が確認し、条件を満たした候補だけを `reports` の非公開投稿へ作成できるようにする。

## 今回実装したこと

- 候補審査DBの各候補に、非公開投入チェックを表示する。
- 出典確認、公開サマリー確認、建物確認、法務・表現確認、判断、Hidden固定、未作成状態をスコア化する。
- すべての条件を満たす候補だけ、管理画面から非公開投稿を作成できる。
- 作成される投稿は `pending` または `needs_review` のままで、`approved` にはしない。
- 証拠レベルは必ず `Hidden` として作成する。
- 候補の `linked_report_id` に作成済み投稿を紐付け、重複作成を避ける。
- `admin_actions` に、成功、重複紐付け、条件未達の操作ログを残す。

## 公開しない情報

- 投稿者メールアドレス
- 非公開メモ
- 証拠画像URL
- Storage bucket / path
- 外部口コミやニュース本文の転載
- 個人名、顔写真、電話番号、SNS ID

## 非公開投稿作成の条件

1. 出典URL・確認日を確認済み
2. 公開サマリーが独自要約
3. 住所・建物・階数を確認済み
4. 法務・表現確認が `approved_for_import`
5. 判断が `import_private`
6. `evidence_level = Hidden`
7. `proposed_status` が `pending` または `needs_review`
8. まだ `linked_report_id` がない

## 運用メモ

非公開投稿を作成しても、公開承認ではない。公開する場合は、投稿詳細画面で別途、人間が公開サマリー、リスクタグ、証拠レベル、異議申立てリスクを確認してから承認する。

## 次に厚くする箇所

- 候補ごとの再確認期限
- 候補の一括フィルタ
- 出典リンクの定期再確認ログ
- 公開承認前の二名確認フラグ
- エリア別の候補消化率ダッシュボード
