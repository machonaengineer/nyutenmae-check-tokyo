# データ品質SOP

このSOPは、初期データ投入、投稿審査、同一住所・同一建物候補の確認を行うための運用手順です。

## 基本方針

- 投稿は `pending` または `needs_review` から開始し、自動公開しない。
- 証拠レベルは、管理者確認前は `Hidden` を維持する。
- 投稿者メール、証拠画像URL、Storageパス、非公開メモは公開サマリーに入れない。
- 同一住所・同一建物の候補は、同一運営や同一店舗であることを断定しない。
- 外部口コミ、ニュース本文、SNS投稿本文は転載は禁止し、出典URL、確認日、独自要約で扱う。

## 毎日の確認

1. `/admin/quality` を開く。
2. `7日超未審査` を確認し、公開、非公開、差し戻し、却下を判断する。
3. `未対応申立て` がある場合は、対象投稿を優先して確認する。
4. `建物名不足` と `階数不足` を確認し、分かる範囲で補完する。
5. `出典確認待ち` は、出典URL、確認日、独自要約、転載有無を確認する。

## 同一住所・同一建物候補

1. `/admin/quality` の `同一住所・同一建物の候補` を確認する。
2. 各投稿を開き、住所、建物名、階数、出典、証拠を個別に確認する。
3. 類似報告として扱う場合も、同一運営や同一店舗とは断定しない。
4. 必要な場合だけ `同一住所・同一建物で類似報告あり` タグを付与する。
5. 公開サマリーは「投稿者の申告では」「確認中です」などの表現にする。

## 初期データ投入

1. `INITIAL_DATA_TEMPLATE.csv` に、出典URL、確認日、独自要約を入力する。
2. 住所が分かる場合は、建物名と階数も確認する。
3. 実名入り候補CSVは公開リポジトリに入れず、まず `/admin/data` の候補審査DBへ登録する。
4. 候補審査DBでは、出典確認、独自要約確認、建物確認、法務・表現確認を完了させる。
5. `import_private` は公開承認ではなく、非公開投稿として既存の審査フローへ進める判断として扱う。
6. `/admin/data` で検証し、エラーを0件にする。
7. 警告は人間が確認し、必要ならCSVを修正する。
8. 投入後は `/admin/reports` で `needs_review` または `pending` として審査する。

## 公開前チェック

- 公開サマリーに禁止表現、個人情報、外部本文コピーがない。
- 証拠画像や投稿者メールが公開ページに出ない。
- 異議申立てがある場合は確認を優先している。
- 承認する投稿だけ `approved` にする。
- `approved` 以外は公開ページ、地図、検索結果に表示しない。
