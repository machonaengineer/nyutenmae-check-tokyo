# フェーズ21: 初期データ投入とSEO流入の土台作り

## 目的

情報が少ない状態から、根拠のない店舗情報を作らず、公的情報、報道、投稿、管理者確認メモを安全に蓄積する。

## 実装済み

- `/coverage` で情報ソースとエリア別の蓄積状況を公開する。
- `/sources` で出典の全体件数、公式・公的情報、報道、審査待ち候補の件数を表示する。
- `/admin/research` で出典キュー、候補化状況、次に審査する候補を確認する。
- `INITIAL_DATA_REVIEW_QUEUE.csv` で、既存の初期データ候補を人間が確認する順番を管理する。
- `PRODUCT_GOAL_AND_ARCHITECTURE.md` に最終目標、非目標、データライフサイクルを固定する。

## 運用手順

1. `/admin/research` で `審査待ち候補` を確認する。
2. `INITIAL_DATA_REVIEW_QUEUE.csv` の上から順に、出典URL、確認日、現在状況、店名、住所、建物名、階数を確認する。
3. 店名または住所が不明確な候補は、個別店舗として承認せず、エリア注意喚起やチェックリスト改善に回す。
4. 承認候補は `/admin/data` から `pending` または `needs_review`、証拠レベル `Hidden` で投入する。
5. `/admin/quality` で建物情報不足、同一住所・同一建物候補、異議申立て状況を確認する。
6. `/admin/reports/[id]` で公開サマリーとリスクタグを整え、承認できるものだけ `approved` にする。

## 公開しないもの

- 未承認投稿。
- 投稿者メール、証拠画像URL、Storageパス、非公開メモ。
- 外部口コミ本文、ニュース本文、SNS本文、スクリーンショット。本文転載は禁止とする。
- 店員個人名、顔写真、電話番号、SNS ID。
- 同一運営、同一店舗、法令違反を断定する表現。

## 次の実データ投入基準

- 出典URLが現在も確認できる。
- 出典確認日が残っている。
- 公開サマリーが独自要約になっている。
- 個別店舗として扱う場合、店名、住所、建物名、階数の確認状況が明確。
- 異議申立て導線が表示される。
- 公開前に人間が法務・表現・現在状況を確認している。
