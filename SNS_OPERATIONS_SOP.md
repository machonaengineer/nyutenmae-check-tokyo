# SNS運用SOP

## 目的

「入店前チェック東京」のSNS運用は、個別店舗や個人への断定ではなく、入店前確認、相談導線、情報提供募集を広げるために行う。

## 運用原則

- 証拠画像、投稿者メールアドレス、非公開メモをSNSに載せない。
- 外部口コミ本文、ニュース本文、SNS投稿本文、スクリーンショットを転載しない。
- 個別店舗や個人への断定、攻撃、煽り表現を使わない。
- 個別報告を紹介する場合は、承認済み公開ページのURLだけを共有する。
- 投稿や返信で未確認情報が出た場合は、公開返信で詳細を扱わず投稿フォームへ誘導する。
- 身の危険がある相談には、SNS上の議論より安全確保と相談導線を優先する。

## 毎日の型

1. 朝: 保存されやすい入店前チェックを投稿する。
2. 昼: エリアページ、公式確認先、相談導線のいずれかを投稿する。
3. 夜: 情報提供募集、投稿方針、週末前確認を投稿する。
4. 返信: 個別店舗名が出たら、断定せず投稿フォームまたは異議申立て導線へ案内する。
5. 記録: `SNS_KPI_LOG_TEMPLATE.csv` の列に沿って、伸びた投稿、保存されそうな投稿、問い合わせにつながった投稿をメモする。

## 日次KPI記録

- `SNS_KPI_LOG_TEMPLATE.csv` をコピーして、日付、投稿枠、投稿URL、遷移先URL、表示回数、プロフィールクリック、リンククリック、保存、情報提供フォーム遷移を記録する。
- 個別店舗名、投稿者メールアドレス、非公開メモ、証拠画像URL、外部本文は記録しない。
- 数字が少ない間は、いいね数よりもリンククリック、保存、情報提供フォーム遷移を優先して見る。
- 週1回、保存型、相談型、エリア型、方針型、募集型のどれが伸びたかを確認し、翌週の投稿カレンダーへ反映する。

## 伸ばすための投稿型

- 保存型: 「入店前に確認したいこと」などチェックリスト形式。
- 相談型: 110、#9110、188、カード会社、保存すべき資料。
- エリア型: 掲載対象エリアごとの確認ページ。
- 仕組み型: 掲載方針、情報提供フォーム、異議申立て導線。
- 募集型: 具体的な経緯、明細の有無、相談状況の情報提供募集。

## 文面テストルール

- 朝投稿は14日間、同一文面を繰り返さない。
- 冒頭はサービス説明ではなく、「保存用」「3つだけ」「捨てる前に保存」など、読む理由が1文目で分かる形にする。
- 1投稿につき価値とCTAを1つに絞り、保存型、相談型、検索型、仕組み型、募集型をローテーションする。
- ハッシュタグは常用せず、会話調で簡潔に書く。
- 投稿から24時間後に表示回数、エンゲージメント率、プロフィールクリック、リンククリック、情報提供フォーム遷移を記録する。
- 週次で投稿型ごとの中央値を比較し、反応が弱い文面は言い換えではなく、フックまたは導線から変更する。
- 画像や動画を追加する場合は、権利、個人情報、EXIF、可読性を別途確認し、テキスト投稿の承認だけで自動追加しない。

## 投稿前チェック

- 禁止表現が入っていない。
- 個人情報、証拠画像URL、Storageパスが入っていない。
- 投稿者メールアドレスや管理者メモが入っていない。
- 断定ではなく「確認」「申告」「注意情報」の表現になっている。
- 外部本文や画像の転載は禁止を守っている。
- リンク先が公開ページであり、管理画面や非公開URLではない。

## 初週の優先順位

1. 固定ポストを設定する。
2. `/social` と `/contribute` を毎日1回ずつ露出する。
3. 各エリアページを1日1つ紹介する。
4. 相談導線 `/support` を週2回以上紹介する。
5. 投稿が来たら、公開せず管理画面で審査する。

## 自動化しないこと

- 自動いいね、自動フォロー、自動リプライ。
- 複数アカウントを使った不自然な拡散。
- 外部サイト本文の転載。
- 未承認投稿の紹介。
- 個別店舗への断定的なラベル付け。

## 将来の自動化候補

X APIなど公式APIを使う場合は、アクセストークンをサーバー専用環境変数に置き、投稿前の管理者承認、操作ログ、失敗時の再送制御を必須にする。

## 自動投稿キュー運用

- `npm run social:prepare -- --date=YYYY-MM-DD --slot=morning` で、`SOCIAL_CONTENT_CALENDAR.csv` から安全確認済みの日次投稿を `approved` として生成する。
- 個別に作る文面は `SNS_AUTO_POST_QUEUE.csv` に `draft` として作り、投稿前チェック後に `status=approved` へ変更する。
- `npm run social:autopost -- --dry-run --date=YYYY-MM-DD --slot=morning` で文面とリンクを確認する。
- `SNS_AUTO_POST_ENABLED=true` の時だけ、`scripts/social-autopost.mjs` がX公式APIへ投稿する。
- 公式APIトークンが無い暫定運用では、Chromeで表示アカウントが `@nyutenmaecheck` であることを確認できた時だけ、dry-runと同じ文面を投稿する。
- 投稿後はキューの対象行を `posted` に更新し、`posted_at` と `post_url` を記録する。
- 失敗した行は再実行前に文面、API利用枠、X側のアプリ権限を確認する。

## 返信案内キュー運用

- 対象投稿の本文は転載せず、`SNS_REPLY_QUEUE.csv` にURL、投稿ID、確認メモだけを残す。
- 返信は、相手が対象アカウントを@メンションまたは引用で呼んだ場合だけ `summoned_account=yes` にする。
- 返信前チェック後、管理者が `status=approved` に変更する。
- `npm run social:reply -- --dry-run --tweet-id=1234567890` で文面、リンク、禁止表現、非公開情報マーカーを確認する。
- `SNS_AUTO_REPLY_ENABLED=true` の時だけ、`scripts/social-reply.mjs` がX公式APIへ返信する。
- 返信文は、情報提供フォーム、相談先、保存資料の案内に限定し、個別店舗や個人を評価しない。
