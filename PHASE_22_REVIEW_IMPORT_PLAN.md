# フェーズ22: 審査キューから非公開投入までの短縮

## 目的

初期データ候補を、公開せず、管理者だけが確認できる `needs_review / Hidden` の投稿として安全に投入できるようにする。

## 実装済み

- 初期データ候補CSVを `server-only` の管理用モジュールに移した。
- `/admin/data` に初期データ審査キューを表示した。
- `/admin/data` に「候補を非公開投入する」ボタンを追加した。
- 候補投入はサーバーアクションで実行し、クライアントコンポーネントへ候補CSVを渡さない。
- 投入時は既存バリデーションを通し、`pending` または `needs_review`、証拠レベル `Hidden` のみ許可する。
- 重複候補は既存の重複判定でスキップする。

## 運用ルール

1. `/admin/data` の審査キューで、出典URL、確認日、店名、住所、建物名、階数を確認する。
2. まだ公開判断できない候補は、そのまま `needs_review / Hidden` で非公開投入する。
3. 投入後は `/admin/quality` で建物情報不足、出典確認待ち、類似候補を確認する。
4. `/admin/reports/[id]` で公開サマリー、リスクタグ、証拠レベル、異議申立て導線を確認する。
5. 公開できる場合だけ人間が `approved` に変更する。

## 公開しないもの

- 未承認候補。
- 候補CSVの非公開メモ。
- 投稿者メール、証拠画像URL、Storageパス。
- 外部本文、ニュース本文、SNS本文、スクリーンショット。
- 同一運営や法令違反を断定する表現。

## 次の改善

- 候補ごとのチェック完了状態をDB化する。
- 出典URLの到達確認を管理画面から実行できるようにする。
- 建物名と階数の補完候補を管理画面に表示する。
